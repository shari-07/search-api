/*
import { Elysia } from 'elysia'
import { handleOAuthCallback, getOAuthStatus } from '../controllers/oauth.controller'

const oauthRoutes = new Elysia({ prefix: '/oauth' })
  .get('/callback', handleOAuthCallback)
  .get('/status', getOAuthStatus)

export default oauthRoutes
*/